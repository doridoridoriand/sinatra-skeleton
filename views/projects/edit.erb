<div class="row mb-3">
  <div class="col-12 d-flex align-items-center justify-content-between">
    <div>
      <p class="text-uppercase text-muted small mb-1">Projects</p>
      <h1 class="h3 mb-0">Edit: <%= @project[:name] %></h1>
    </div>
    <a class="btn btn-outline-secondary" href="/projects/<%= @project[:id] %>">View</a>
  </div>
</div>

<div class="row">
  <div class="col-lg-8">
    <div class="card">
      <div class="card-body">
        <%= erb :"projects/_form", locals: { project: @project, form_action: "/projects/#{@project[:id]}", http_method: "patch", submit_label: "Save changes" } %>
      </div>
    </div>
  </div>
  <div class="col-lg-4">
    <div class="card h-100">
      <div class="card-body">
        <h2 class="h6">Need to delete?</h2>
        <p class="text-muted">削除すると元に戻せません。削除は一覧ページから行えます。</p>
      </div>
    </div>
  </div>
</div>
